Releasing a new version

# consider regenerating images using docs/images/readme_images.ipynb
  make readme
  git commit docs/images/*png -m 'updates to readme images'

# run release check
  make rcheck

# update the version in __init__.py
  git commit -m 'update version' laserbeamsize/__init__.py

# update jupyterlite pages on github
  make lite-clean
  make lite
  make lite-deploy

# update CHANGELOG.rst  `git shortlog 2.1..HEAD`
  git commit -m 'update recent changes' CHANGELOG.rst
  git push

# create release tag on github
  git tag 2.2.0
  git push origin 2.2.0

# manually publish release at
  open https://github.com/scottprahl/laserbeamsize
  
# verify action success
  open https://doi.org/10.5281/zenodo.8346799
  open https://pypi.org/project/laserbeamsize/
  open https://laserbeamsize.readthedocs.io
  
# wait for feedstock update
  open https://github.com/conda-forge/laserbeamsize-feedstock
